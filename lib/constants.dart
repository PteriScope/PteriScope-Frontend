import 'package:flutter/material.dart';

class AppConstants {
  static const int splashDelay = 2;
  static const Color primaryColor = Color(0xFF475BD8);
  static const double padding = 16.0;
  static const String endpoint = "http://localhost:8080/api";
}
